apiVersion: v1
kind: Service
metadata:
  name: finance-analytics
  namespace: finance-analytics
spec:
  selector:
    app: finance-analytics
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
