# This file is an example only — do NOT commit real values.
# Create the secrets imperatively (see k8s/README.md):
#
#   kubectl create secret generic gcp-credentials \
#     --from-file=credentials.json=/path/to/service-account-key.json \
#     --namespace=finance-analytics
#
#   kubectl create secret generic finance-analytics-secrets \
#     --from-literal=BQ_PROJECT=your-gcp-project-id \
#     --namespace=finance-analytics
#
# On GKE, replace both secrets with Workload Identity (no key file needed).
# See k8s/README.md for instructions.
